<div class="row">
    <h3>Student Grade Report App</h3>
</div>
<div class="row">
    <p>
        <a href="/create" class="btn btn-success">Create</a>
    </p>
    <table class="table table-striped table-dark">
    <thead>
        <tr>
        <th scope="col">Student Id</th>
        <th scope="col">Student Name</th>
        <th scope="col">Student Percentage</th>
        <th scope="col">Student Letter Grade</th>
        </tr>
    </thead>
        <tbody>
        {{# each students}}
            <tr>
                <th scope="row">{{student.id}}</th>
                <td>{{student.studentName}}</td>
                <td>{{student.studentPercent}}</td>
                <td>{{student.studentLetterGrade}}</td>
                <td>
                    <a class="btn" href="/read?id=" + {{students.id}}>Read</a>
                    <a class="btn" href="/edit?id=" + {{students.id}}>Edit</a>
                    <a class="btn" href="/delete?id=" + {{students.id}}>Delete</a>
                </td>
            </tr>
        {{/each}}
        </tbody>
    </table>
</div>